# Projects presentation website in Vitepress

## Launch the project

At the root of the project, run the following command:
```bash
npm run docs:dev
```
or 
```bash
yarn docs:dev
``` 

## Build the project

At the root of the project, run the following command:
```bash
npm run docs:build
```
or 
```bash
yarn docs:build
```
